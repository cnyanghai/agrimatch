<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agrimatch.supply.mapper.SupplyMapper">

    <resultMap id="BusSupplyMap" type="com.agrimatch.supply.domain.BusSupply">
        <id column="id" property="id"/>
        <result column="company_id" property="companyId"/>
        <result column="user_id" property="userId"/>
        <result column="company_name" property="companyName"/>
        <result column="user_name" property="userName"/>
        <result column="nick_name" property="nickName"/>
        <result column="category_name" property="categoryName"/>
        <result column="supply_no" property="supplyNo"/>
        <result column="origin" property="origin"/>
        <result column="quantity" property="quantity"/>
        <result column="ex_factory_price" property="exFactoryPrice"/>
        <result column="ship_address" property="shipAddress"/>
        <result column="delivery_mode" property="deliveryMode"/>
        <result column="packaging" property="packaging"/>
        <result column="storage_method" property="storageMethod"/>
        <result column="price_rules_json" property="priceRulesJson"/>
        <result column="params_json" property="paramsJson"/>
        <result column="remark" property="remark"/>
        <result column="expire_minutes" property="expireMinutes"/>
        <result column="expire_time" property="expireTime"/>
        <result column="status" property="status"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.agrimatch.supply.domain.BusSupply"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO bus_supply
        (company_id, user_id, category_name, supply_no, origin, quantity, ex_factory_price, ship_address,
         delivery_mode, packaging, storage_method,
         price_rules_json, params_json, remark, expire_minutes, expire_time,
         status, is_deleted, create_time, update_time)
        VALUES
        (#{companyId}, #{userId}, #{categoryName}, #{supplyNo}, #{origin}, #{quantity}, #{exFactoryPrice}, #{shipAddress},
         #{deliveryMode}, #{packaging}, #{storageMethod},
         #{priceRulesJson}, #{paramsJson}, #{remark}, #{expireMinutes}, #{expireTime},
         #{status}, 0, NOW(3), NOW(3))
    </insert>

    <select id="selectById" resultMap="BusSupplyMap">
        SELECT
            s.id, s.company_id, s.user_id,
            c.company_name AS company_name,
            u.user_name AS user_name,
            u.nick_name AS nick_name,
            s.category_name, s.supply_no, s.origin, s.quantity, s.ex_factory_price, s.ship_address,
            s.delivery_mode, s.packaging, s.storage_method,
            s.price_rules_json, s.params_json, s.remark, s.expire_minutes, s.expire_time,
            s.status, s.is_deleted, s.create_time, s.update_time
        FROM bus_supply s
        LEFT JOIN bus_company c ON c.id = s.company_id AND (c.is_deleted IS NULL OR c.is_deleted = 0)
        LEFT JOIN sys_user u ON u.user_id = s.user_id AND u.del_flag = '0'
        WHERE s.id = #{id}
          AND s.is_deleted = 0
          AND (s.expire_time IS NULL OR s.expire_time > NOW(3) OR s.status = 1)
        LIMIT 1
    </select>

    <select id="selectList" resultMap="BusSupplyMap">
        SELECT
            s.id, s.company_id, s.user_id,
            c.company_name AS company_name,
            u.user_name AS user_name,
            u.nick_name AS nick_name,
            s.category_name, s.supply_no, s.origin, s.quantity, s.ex_factory_price, s.ship_address,
            s.delivery_mode, s.packaging, s.storage_method,
            s.price_rules_json, s.params_json, s.remark, s.expire_minutes, s.expire_time,
            s.status, s.is_deleted, s.create_time, s.update_time
        FROM bus_supply s
        LEFT JOIN bus_company c ON c.id = s.company_id AND (c.is_deleted IS NULL OR c.is_deleted = 0)
        LEFT JOIN sys_user u ON u.user_id = s.user_id AND u.del_flag = '0'
        <where>
            s.is_deleted = 0
            <if test="q == null or q.includeExpired == null or q.includeExpired == false">
                AND (s.expire_time IS NULL OR s.expire_time > NOW(3) OR s.status = 1)
            </if>
            <if test="q != null and q.status != null">
                AND s.status = #{q.status}
            </if>
            <if test="q != null and q.companyId != null">
                AND s.company_id = #{q.companyId}
            </if>
            <if test="q != null and q.userId != null">
                AND s.user_id = #{q.userId}
            </if>
            <if test="q != null and q.categoryName != null and q.categoryName != ''">
                AND s.category_name LIKE CONCAT('%', #{q.categoryName}, '%')
            </if>
        </where>
        ORDER BY
        <choose>
            <when test="q != null and q.orderBy == 'create_time'">
                s.create_time
            </when>
            <when test="q != null and q.orderBy == 'ex_factory_price'">
                s.ex_factory_price
            </when>
            <when test="q != null and q.orderBy == 'distance'">
                s.create_time
            </when>
            <when test="q != null and q.orderBy == 'delivered_price'">
                s.ex_factory_price
            </when>
            <otherwise>
                s.create_time
            </otherwise>
        </choose>
        <choose>
            <when test="q != null and q.order == 'asc'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
    </select>

    <update id="update" parameterType="com.agrimatch.supply.domain.BusSupply">
        UPDATE bus_supply
        <set>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="supplyNo != null">supply_no = #{supplyNo},</if>
            <if test="origin != null">origin = #{origin},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="exFactoryPrice != null">ex_factory_price = #{exFactoryPrice},</if>
            <if test="shipAddress != null">ship_address = #{shipAddress},</if>
            <if test="deliveryMode != null">delivery_mode = #{deliveryMode},</if>
            <if test="packaging != null">packaging = #{packaging},</if>
            <if test="storageMethod != null">storage_method = #{storageMethod},</if>
            <if test="priceRulesJson != null">price_rules_json = #{priceRulesJson},</if>
            <if test="paramsJson != null">params_json = #{paramsJson},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="expireMinutes != null">expire_minutes = #{expireMinutes},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW(3)
        </set>
        WHERE id = #{id}
          AND user_id = #{userId}
          AND is_deleted = 0
    </update>

    <update id="logicalDelete">
        UPDATE bus_supply
        SET is_deleted = 1,
            update_time = NOW(3)
        WHERE id = #{id}
          AND user_id = #{userId}
          AND is_deleted = 0
    </update>

    <update id="expireDownShelf">
        UPDATE bus_supply
        SET status = 1,
            update_time = NOW(3)
        WHERE is_deleted = 0
          AND status = 0
          AND expire_time IS NOT NULL
          AND expire_time &lt;= NOW(3)
    </update>

</mapper>


