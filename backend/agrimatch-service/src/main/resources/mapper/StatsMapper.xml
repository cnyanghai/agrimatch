<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agrimatch.home.mapper.StatsMapper">

    <select id="userCount" resultType="long">
        SELECT COUNT(1)
        FROM sys_user
        WHERE del_flag = '0'
          AND (is_deleted IS NULL OR is_deleted = 0)
    </select>

    <select id="requirementCount" resultType="long">
        SELECT COUNT(1)
        FROM bus_requirement
        WHERE is_deleted = 0
    </select>

    <select id="supplyCount" resultType="long">
        SELECT COUNT(1)
        FROM bus_supply
        WHERE is_deleted = 0
    </select>

    <select id="supplierCount" resultType="long">
        SELECT COUNT(DISTINCT company_id)
        FROM bus_supply
        WHERE is_deleted = 0
    </select>

    <select id="buyerCount" resultType="long">
        SELECT COUNT(DISTINCT company_id)
        FROM bus_requirement
        WHERE is_deleted = 0
    </select>

</mapper>

